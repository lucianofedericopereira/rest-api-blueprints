# Copy to .env and fill in real values.
# A.10: Never commit secrets — see .gitignore.

APP_NAME=iso27001-gin
APP_VERSION=1.0.0
APP_ENV=development
PORT=8003

# A.10: JWT — generate with: openssl rand -hex 32
JWT_SECRET=change-me-in-production
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# A.10: AES-256-GCM — must be exactly 32 bytes
ENCRYPTION_KEY=dev-only-32-byte-key-change-me!!

# A.9: CORS — comma-separated, no wildcard in production
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Database (PostgreSQL)
DATABASE_URL=host=localhost user=user password=pass dbname=iso27001 sslmode=disable

# Redis (optional — graceful in-process fallback if unavailable)
REDIS_URL=redis://localhost:6379/0
